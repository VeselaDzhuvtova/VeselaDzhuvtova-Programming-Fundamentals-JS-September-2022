function introduse(studentName, studentAge, studentGrade) {
    let introduction = `Name: ${studentName}, Age: ${studentAge}, Grade: ${studentGrade.toFixed(2)}`;
    console.log(introduction)
}
introduse('Jonh',15, 5.54678);