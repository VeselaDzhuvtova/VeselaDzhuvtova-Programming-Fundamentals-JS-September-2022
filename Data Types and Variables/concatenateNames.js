function concatenateNames (first, second, delimiter) {
    let firstResult = first + delimiter + second;
    //let secondResult = `${firstResult}${delimiter}${second}`

    console.log(firstResult);
}
concatenateNames('John','Smith','->');