    function RequiredReading(currentBookPages, pagesForHour, days) {

        let totalHours = currentBookPages / pagesForHour;
        let hours = totalHours / days

        console.log(hours)

    }
RequiredReading ('212', '20', '2')